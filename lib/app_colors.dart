import 'package:flutter/material.dart';

/// Dark green used as the app's primary color throughout.
const Color appPrimaryGreen = Color.fromARGB(255, 20, 73, 24);

/// Lighter variant for icons and accents (e.g. replacing green.shade400).
const Color appPrimaryGreenLight = Color.fromARGB(255, 50, 95, 52);

/// Very light background variant (e.g. replacing green.shade100).
final Color appPrimaryGreenLightBg = const Color.fromARGB(
  255,
  226,
  245,
  227,
).withOpacity(0.2);
